{% extends 'base.html.twig' %}

{% block title %}Nueva Galeria{% endblock %}

{% block body %}

    <div class="row">
        <div class="col-md-12">
            {{ include('web_galeria/_form.html.twig') }}
        </div>
    </div>

    <a class="btn btn-secondary" href="{{ path('web_galeria_index') }}">Volver</a>
{% endblock %}
