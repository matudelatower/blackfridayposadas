{% extends '/Web/layout.html.twig' %}
{% block meta_description %}Sección rubros de los comercios adheridos Black Frdiday 2019{% endblock %}
{% block title %}Rubros - {% endblock %}

{% block content %}

    <section class="page">
        <div class="container">
            <div class="row align-items-center">

                <div class="col-md-12 p-5">
                    <h2 class="display-4">Rubros</h2>
                    <div class="list-group">
                        {% for rubro in rubros %}
                            <a href="{{ path('comercios', {'rubroId': rubro.id}) }}" class="list-group-item d-flex justify-content-between align-items-center">
                                {{ rubro }}
                                <span class="badge badge-primary badge-pill">{{ rubro.comercios | length }}</span>
                            </a>
                        {% endfor %}
{#                        <li class="list-group-item d-flex justify-content-between align-items-center">#}
{#                            Cras justo odio#}
{#                            <span class="badge badge-primary badge-pill">14</span>#}
{#                        </li>#}
{#                        <li class="list-group-item d-flex justify-content-between align-items-center">#}
{#                            Dapibus ac facilisis in#}
{#                            <span class="badge badge-primary badge-pill">2</span>#}
{#                        </li>#}
{#                        <li class="list-group-item d-flex justify-content-between align-items-center">#}
{#                            Morbi leo risus#}
{#                            <span class="badge badge-primary badge-pill">1</span>#}
{#                        </li>#}
                    </div>
                </div>
            </div>
        </div>
    </section>

{% endblock content %}