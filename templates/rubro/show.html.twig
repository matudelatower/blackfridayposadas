{% extends 'base.html.twig' %}

{% block title %}Rubro{% endblock %}

{% block body %}
    <h1>Rubro</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rubro.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ rubro.nombre }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{{ rubro.activo ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>FechaCreacion</th>
                <td>{{ rubro.fechaCreacion ? rubro.fechaCreacion|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>FechaActualizacion</th>
                <td>{{ rubro.fechaActualizacion ? rubro.fechaActualizacion|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('rubro_index') }}">back to list</a>

    <a href="{{ path('rubro_edit', {'id': rubro.id}) }}">edit</a>

    {{ include('rubro/_delete_form.html.twig') }}
{% endblock %}
